      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }@      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }F      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }×      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }×      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }×      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }×      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }×      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }×      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }×      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }×      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }O      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }U      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }K      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }Y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }F      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }O      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }q      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }q      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }!      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }O      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }O      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }O      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }q      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }q      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }q      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }q      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }F      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }O      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }q      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }O      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }q      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }q      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }q      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }O      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }O      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }F      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }|      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }F      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }q      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }!      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }O      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }+      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }+      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }+      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }+      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }U      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }U      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }U      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }F      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }U      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }?      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }X      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }U      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }O      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }K      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }F      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }F      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }F      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }F      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }|      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }|      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }|      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }|      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }|      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }|      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }F      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }!      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }!      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }!      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }F      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }F      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }F      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }+      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }V      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }!      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }?      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }?      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }!      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }O      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }O      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }K      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }Y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }Y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }%      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }+      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }%      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }Y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }V      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }V      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }V      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }%      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }V      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }%      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }|      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }|      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }%      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }%      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }!      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }!      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }F      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }O      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }+      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }O      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }|      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }|      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }O      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }×      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }O      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }+      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }+      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }U      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }F      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }X      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }!      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }F      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }!      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }V      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }V      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }V      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }|      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }|      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }#      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }!      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }!      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }#      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }#      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }#      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }!      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }?      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }V      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }V      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }#      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }#      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }?      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }#      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }#      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }#      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }#      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }Z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }V      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }V      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }Z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }#      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }#      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }#      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }?      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }#      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }#      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }#      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }$      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }%      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }O      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }%      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }~      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }F      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }$      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }O      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }|      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }|      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }|      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }|      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }|      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }|      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }$      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }|      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }|      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }$      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }?      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }$      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }?      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }!      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }?      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }?      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }%      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }$      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }$      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }|      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }|      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }%      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }?      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }?      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }U      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }X      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }%      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }$      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }O      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }X      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }%      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }$      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }O      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }$      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }?      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }$      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }?      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }K      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }$      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }?      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }$      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }?      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }K      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }$      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }?      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }$      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }?      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }$      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }?      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }$      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }?      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }U      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }U      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }$      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }$      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }U      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }O      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }U      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }X      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }F      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }X      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }Y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }Y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }%      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }O      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }Y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }O      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }O      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }Y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }O      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }O      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }F      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }X      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }+      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }Y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }O      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }U      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }O      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }Y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }F      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }X      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }O      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }|      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }|      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }?      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }O      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }|      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }|      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }F      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }O      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }F      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }F      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }F      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }_      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }!      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }×      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }?      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }W      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }$      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }?      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }'      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }U      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }E      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }O      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }D      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }A      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }*      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }&      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }[      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }]      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }S      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }9      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }7      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }z      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }6      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }N      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }=      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }"      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }Y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }P      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }b      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }O      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }F      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }Y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }Y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }×      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }L      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }Y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }×      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }×      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }R      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }M      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }G      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }H      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }g      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }h      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }C      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }Y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }B      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }×      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }2      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      },      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }5      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }T      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }j      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }@      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }U      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }Y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }%      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }Y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }@      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }k      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }U      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }Y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }x      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }8      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }Y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }c      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }(      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }1      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }U      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }3      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }{      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }m      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }:      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }I      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }n      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }U      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }p      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }0      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }.      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }4      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }-      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }u      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }f      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }o      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }w      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }a      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }r      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }`      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }s      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }t      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }y      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }l      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }e      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }<      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }/      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }d      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }i      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }v      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }>      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }       if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      })      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      };      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }}      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }
      if (balance >= betAmt) {
        const newBalance = balance - betAmt;
        setBalance(newBalance);
        walletDB.setCoins(newBalance);
        setBets(prev => ({ ...prev, [fruitId]: (prev[fruitId] || 0) + betAmt }));
      }